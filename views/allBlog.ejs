<!DOCTYPE html>
<html lang="en" >

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Blogs || Global Blogs</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    /* Custom styles */
    

    .card {
      margin-bottom: 20px;
      border: none;
      border-radius: 15px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .card:hover {
      transform: translateY(-5px);
    }

    .card img {
      /* border-top-left-radius: 15px;
      border-top-right-radius: 15px; */
      height: 200px;
      object-fit: cover;
    }

    .card-body {
      padding: 20px;
    }

    .card-title {
      font-size: 24px;
      color: #333;
    }

    .card-text {
      font-size: 18px;
      color: #666;
    }

    .btn-primary {
      background-color: #4CAF50;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      font-size: 18px;
      transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #45a049;
    }
  </style>
</head>

<body  >

  <%- include('./partials/navbar')%>
  <center ><br>
    <div style="width: 80%;"><h1 >All Blogs</h1><hr></div>
</center>
  <div class="container mt-4">
    <div class="row">
      <% blogs.forEach(function(blog, index) { %>
        <div class="col-md-6 ">
          <div class="card"> <img src="<%= blog.titleImage %>" class="card-img-top" alt="<%= blog.titleImage %>">
            <div class="card-body">
              <h5 class="card-title">
                <%= blog.title %>
              </h5>
              <p class="card-text">
                <%= blog.desc %>
              </p> <a href="/showBlog/<%=blog._id%>" class="btn btn-outline-warning">Read More</a>
            </div>
          </div>
        </div>
        <% if (index % 2===1) { %>
          <div class="w-100"></div> <!-- Add a new row after every two cards -->
          <% } %>
            <% }); %>
    </div>
  </div>
  <%- include('./partials/footer')%>
  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>

</html>



 